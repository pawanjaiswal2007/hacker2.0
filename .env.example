#############################
# Environment variables example
# Copy this file to `.env.local` and replace the placeholder
# values with your real credentials. Do NOT commit `.env.local`.
#############################

# JWT signing secret used to sign auth tokens (use a long random string)
SECRET_JWT=replace_with_a_secure_random_string

# MongoDB connection string (MongoDB Atlas format). Do NOT commit real creds.
# Example: DB=mongodb+srv://alice:password@cluster0.mongodb.net/mydbname?retryWrites=true&w=majority
DB=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>

# Optional: Google OAuth client ID (used to validate Google ID tokens)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: Upstash Redis REST credentials (used by the rate limiter)
UPSTASH_REDIS_REST_URL=https://<region>.upstash.io/v1/<rest-path>
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token

# Notes:
# - Create a `.env.local` file at the project root with the real values.
# - Restart the dev server after changing environment variables.
# - Rotate any credentials that were accidentally committed to the repo.

